\* Out-of-contract discipline run (cascade): deep
\* reorgs permitted. Absorption, monotonicity, frozen-foreclosure
\* stickiness, outbox exclusivity and durable-enqueue are expected
\* to HOLD even though chain-truth invariants fail out of contract.
SPECIFICATION Spec
CONSTANTS
    Txs <- MCTxsFull
    ParentTriggers <- MCParentTriggers
    ChildTriggers <- MCChildTriggers
    SpendsOf <- MCSpendsOf
    PredOK <- MCPredOK
    AnchorOf <- MCAnchorOf
    DependedForm <- MCDependedForm
    Threshold = 2
    StabilityDepth = 2
    MaxLen = 4
    MaxBlocks = 4
    MaxPending = 2
    AlwaysConsume = FALSE
INVARIANTS
    PerformedEnqueued
    MaterializeOnce
PROPERTIES
    SensedTerminalAbsorbing
    DeliveredTerminalAbsorbing
    ViewMonotone
    FcFrozenSticky
